import { ComingSoon, Section } from './ui'

// Notification manager — a future home for managing outgoing notifications.
export default function NotificationPage() {
  return (
    <Section title="Notification" desc="Manage the notifications Tabletsgo sends to your team.">
      <ComingSoon
        title="Notification manager"
        desc="Configure channels and rules for the notifications Tabletsgo sends — landing soon."
      />
    </Section>
  )
}
