import { ComingSoon, Section } from './ui'

export default function S3Page() {
  return (
    <Section title="S3 Storage" desc="Connect object storage buckets to browse and manage files.">
      <ComingSoon title="Object storage" desc="Browse, upload and manage files in your S3-compatible buckets — landing soon." />
    </Section>
  )
}
